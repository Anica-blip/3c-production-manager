// 3C Production Manager — config
// Safe to be public: the worker URL is not a secret. The actual admin
// password lives server-side as a Cloudflare secret, never in this file.

const CONFIG = {
  apiBase: 'https://productionmanager.threadcommand.center/api',
};
